'pip install -r requirements.txt' install all the dependencies for flask 

app.py: creates a new instance of the Flask application, and defines a single 
	route that returns the "Hello World!" statement. 

python --version 3.5.2  

to run your Flask application: 
source venv/bin/activate  // this help you to start to use virtual environment 
export FLASK_APP=app.py 
flask run 

 
